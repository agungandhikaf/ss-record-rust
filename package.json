{
  "name": "mytbc-tauri",
  "version": "1.0.16",
  "private": true,
  "description": "Manual screenshot recorder per TC folder for Tester Budak Corporate using Tauri + Rust.",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:vite": "vite build",
    "dev:vite": "vite --host 127.0.0.1",
    "dist:win": "tauri build --bundles nsis && node scripts/rename-tauri-installer.mjs win",
    "dist:mac": "tauri build --bundles dmg && node scripts/rename-tauri-installer.mjs mac"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.6.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.3",
    "vite": "^5.4.11"
  }
}
